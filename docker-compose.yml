services:
  db:
    image: postgis/postgis:15-3.3
    environment:
      POSTGRES_DB: nextrouter
      POSTGRES_USER: ian
      POSTGRES_PASSWORD: teste.123
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: